# Generated by elixir_baseline. Do not edit: a change made here is lost the
# next time the file is generated. Change the template it comes from instead.
#
# Runs only when the Dockerfile changes, so this must not be a required status
# check.

name: Hadolint

on:
  push:
    branches:
      - main
    paths:
      - "Dockerfile"
  pull_request:
    paths:
      - "Dockerfile"

permissions: {}

jobs:
  hadolint:
    name: Hadolint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: hadolint/hadolint-action@06be81baf89a55ffd0e24b8f04a4185738dd3387 # v3.5.0
        with:
          dockerfile: Dockerfile
